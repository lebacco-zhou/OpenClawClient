<Window x:Class="OpenClawClient.UI.Views.ChatWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="OpenClaw Client" 
        Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource MaterialDesignPaper}">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Messages List -->
        <ListBox x:Name="MessagesListBox"
                Grid.Row="0"
                BorderThickness="0"
                Background="Transparent"
                VirtualizingPanel.IsVirtualizing="True"
                ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                AllowDrop="True"
                Drop="MessagesListBox_Drop"
                PreviewDragOver="MessagesListBox_PreviewDragOver">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Margin="5" Padding="10" CornerRadius="8"
                            Background="{Binding Role, Converter={StaticResource RoleToBackgroundConverter}}">
                        <StackPanel>
                            <TextBlock Text="{Binding Content}" TextWrapping="Wrap"/>
                            <StackPanel Orientation="Horizontal" 
                                       HorizontalAlignment="Right"
                                       Margin="0,5,0,0">
                                <TextBlock Text="{Binding Timestamp, StringFormat='{}{0:HH:mm}'}"
                                          FontSize="10"
                                          Foreground="Gray"/>
                                <TextBlock Text=" 🔒" Visibility="{Binding IsEncrypted, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Input Area -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBox x:Name="InputTextBox"
                    materialDesign:HintAssist.Hint="输入消息... (Ctrl+V 粘贴图片/文件)"
                    AcceptsReturn="True"
                    TextWrapping="Wrap"
                    VerticalScrollBarVisibility="Auto"
                    MaxHeight="150"
                    KeyDown="InputTextBox_KeyDown"/>
            
            <Button x:Name="SendButton"
                   Grid.Column="1"
                   Content="发送"
                   Width="80"
                   Margin="10,0,0,0"
                   VerticalAlignment="Bottom"
                   Click="SendButton_Click"
                   Style="{StaticResource MaterialDesignRaisedButton}"/>
        </Grid>
    </Grid>
</Window>
